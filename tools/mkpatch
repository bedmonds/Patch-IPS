#!/usr/bin/env perl6

constant ips-header = 'PATCH'.encode(:bin);
constant ips-footer = 'EOF'.encode(:bin);

sub MAIN(Str $path) {
	spurt $path, Buf.new(0x50, 0x41, 0x54, 0x43, 0x48, 0x00, 0x02, 0x00, 0x01, 0x05, 0x45, 0x4F, 0x46);
}
